<%- include('header'); -%>

<div class="blogs content">
    <h2>no accept doctors</h2>

    <% if (blogs.length > 0) { %>
      <% blogs.forEach(blog => { %>
        <a class="single" href="/doctordetails/<%= blog._id %>">
          <h3 class="title"><%= blog.email %></h3>
          <p class="snippet"><%= blog.username %></p>
        </a>
      <% }) %>
    <% } else { %>
      <p>There are no doctors to display...</p>
    <% } %>
    
  </div>
 

  <%- include('footer'); -%>